### Maybe this truly is expandable to dozens of schools

I got that rice data insanely fast
There needs to be quotes around every table name, this is important.

For future schools we are going with this Rice Table generator thing because its way more optimized.

I didnt know you could define a function like that...hmmm

def is_all_i(word: str) -> bool:
return all(ch.lower() == "i" for ch in word) and word != ""
